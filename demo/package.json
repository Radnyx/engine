{
  "name": "demo",
  "version": "1.0.1",
  "description": "",
  "scripts": {
    "dev": "cd ../ && npm run build && cd demo && esbuild ./src/index.ts --bundle --outfile=build/main.js --servedir=build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.14.39"
  }
}
